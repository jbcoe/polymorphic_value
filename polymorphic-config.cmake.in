@PACKAGE_INIT@

check_required_components(polymorphic)

if(NOT TARGET polymorphic::polymorphic)
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
    include(${CMAKE_CURRENT_LIST_DIR}/polymorphic-target.cmake)
endif()
